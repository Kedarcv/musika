name: musika
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_webview_plugin: any # Use this line for webview support across platforms
  webview_flutter: ^4.10.0
  # Keep this line if you still need it for mobile

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
